# API к устройству UltraScale+ Architecture Soft Error Mitigation (SEM) Controller

Программа отправляет команды и считывает данные, обрабатывает их и формирует human readable вывод в консоль; позволяет протестировать устройство.

Общение с устройством происходит по UART. Программа используется на отладочной плате: под Linux на ARM архитектуре.

## Не поддерживается:
- SSI устройства и SLR. Данная программа предусмотрена только для одного блока SEM.

## Не гарантируется работоспособность:
- для обычной UltraScale архитектуры. Отличается формат LFA адреса.

## Компиляция программы

Используется компилятор gcc из тулчейна для ARM процессоров, который позволяет выполнить кросс-компиляцию под Linux (arm-gnu-toolchain).

   ```bash
   make
   ```